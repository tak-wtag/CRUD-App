apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
  namespace: crud
spec:
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi